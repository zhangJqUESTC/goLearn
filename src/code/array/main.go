package main

// 数组相关内容
import "fmt"

func main() {
	var a [3]int //数组的类型由元素类型与长度构成, 数组的数量一旦确定就不能更改了，就是说大小不可变
	var b [4]int
	fmt.Printf("value: %d\n", a[0])
	fmt.Printf("value: %d\n", b[0])
	fmt.Println(a)
	// 定义
	// 遍历
	// 二维数组的创建与定义
	// 数组是值类型
}
